# Materiales internos del curso — no forman parte de la web
*.pdf
# …pero los recursos descargables del kit sí se publican
!recursos/*.pdf

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Dependencias (por si en el futuro se añade build)
node_modules/
.vercel

# Secretos y variables de entorno — NUNCA al repositorio
.env
.env.*
*.key
